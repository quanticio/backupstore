[MAIN]
data=D:\Conf LB
repository=D:\backup
# [md5, SHA1, SHA224, SHA256, SHA384, and SHA512]
checksum=md5

[COMPRESSION]
# Compression of repository
# Compression level [1-9] of repository.
# 1 is fastest and produces the least compression, 9 is slowest and produces the most.
# Compression algorithm. [zip, tar, gzip, bz2], zlib use the compression level.
compression=True
compressionLevel=1
compressionAlgo=bz2